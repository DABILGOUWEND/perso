import { SituationPannePipe } from './situation-panne.pipe';

describe('SituationPannePipe', () => {
  it('create an instance', () => {
    const pipe = new SituationPannePipe();
    expect(pipe).toBeTruthy();
  });
});
