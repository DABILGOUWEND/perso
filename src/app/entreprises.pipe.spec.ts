import { EntreprisesPipe } from './entreprises.pipe';

describe('EntreprisesPipe', () => {
  it('create an instance', () => {
    const pipe = new EntreprisesPipe();
    expect(pipe).toBeTruthy();
  });
});
