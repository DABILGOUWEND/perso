import { HightligthDirective } from './hightligth.directive';

describe('HightligthDirective', () => {
  it('should create an instance', () => {
    const directive = new HightligthDirective();
    expect(directive).toBeTruthy();
  });
});
