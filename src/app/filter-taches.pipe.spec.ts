import { FilterTachesPipe } from './filter-taches.pipe';

describe('FilterTachesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTachesPipe();
    expect(pipe).toBeTruthy();
  });
});
